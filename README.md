# Random data insertion to MongoDB

* To install dependencies, run the following command
`npm install`

* To run the project, run the following command
`npm start`

Browse `http://localhost:3000` to insert data into your local mongodb database.
